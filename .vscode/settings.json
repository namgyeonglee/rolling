{
  // 저장할 때 prettier 를 돌려줌
  "editor.formatOnSave": true,
  "editor.codeActionsOnSave": {
    // 저장할 때 import 의 순서를 자동으로 정렬해줌
    "source.organizeImports": "explicit",
    "source.fixAll": "explicit"
  }
}
