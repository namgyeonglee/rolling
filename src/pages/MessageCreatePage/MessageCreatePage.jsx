import { Form } from "./Form";

export function MessageCreatePage() {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        marginTop: "47px",
        marginBottom: "60px",
      }}
    >
      <Form />
    </div>
  );
}
